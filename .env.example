# Ethereum RPC endpoint (example: Sepolia)
RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<alchemy_api_key>

# Deployer: deploys MinimalAccount contract (needs native token for gas)
# Format: 0x + 64 hex chars
DEPLOYER_PRIVATE_KEY=0x<64_hex_chars>

# Sponsor: transfers USDC to Alice (needs native token + USDC)
# Format: 0x + 64 hex chars
SPONSOR_PRIVATE_KEY=0x<64_hex_chars>

# Bundler: used by self-bundled E2E scripts (needs native token for gas)
# Format: 0x + 64 hex chars
BUNDLER_PRIVATE_KEY=0x<64_hex_chars>

# Pimlico API key (https://dashboard.pimlico.io)
# Format: pim_...
PIMLICO_API_KEY=pim_<api_key>

# Alchemy API key (https://dashboard.alchemy.com)
ALCHEMY_API_KEY=<alchemy_api_key>

# Alchemy Gas Manager policy id (UUID)
# Format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
ALCHEMY_GAS_POLICY_ID=<policy_uuid>

# ZeroDev RPCs (from ZeroDev dashboard)
# Example format:
#   https://rpc.zerodev.app/api/v3/<project_uuid>/chain/<chain_id>
ZERODEV_BUNDLER_RPC=https://rpc.zerodev.app/api/v3/<project_uuid>/chain/<chain_id>
ZERODEV_PAYMASTER_RPC=https://rpc.zerodev.app/api/v3/<project_uuid>/chain/<chain_id>

# thirdweb Bundler/Paymaster (see https://portal.thirdweb.com/bundler)
# Required URL format: https://<chain_id>.bundler.thirdweb.com/v2
THIRDWEB_BUNDLER_URL=https://<chain_id>.bundler.thirdweb.com/v2
# Optional if paymaster endpoint differs (default: THIRDWEB_BUNDLER_URL)
THIRDWEB_PAYMASTER_URL=

# thirdweb auth headers (prefer secret key for backend)
# THIRDWEB_CLIENT_ID format: 32-char lowercase hex
THIRDWEB_CLIENT_ID=<32_hex_client_id>
# THIRDWEB_SECRET_KEY format: fk_...
THIRDWEB_SECRET_KEY=fk_<secret_key>
