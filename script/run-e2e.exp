#!/usr/bin/expect -f
set timeout 300

# Pass all args to forge script
spawn {*}$argv

expect {
    "Do you wish to continue?" {
        send "y\r"
        exp_continue
    }
    eof
}

catch wait result
exit [lindex $result 3]
